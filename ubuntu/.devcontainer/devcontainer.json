{
  "name": "Ubuntu",
  "build": {
    "dockerfile": "Dockerfile",
    "args": { "REG": "qbrn.reg", "IMG": "new", "TAG": "std" }
  },
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  // "extensions": ["mutantdino.resourcemonitor", "redhat.vscode-yaml"]
  // "forwardPorts": [],
  // "postCreateCommand": "uname -a",
  // "remoteUser": "vscode"
}
